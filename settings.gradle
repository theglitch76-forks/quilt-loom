pluginManagement {
    repositories {
        maven {
            url = "https://files.minecraftforge.net/maven"
        }
        gradlePluginPortal()
        mavenCentral()
    }
}
rootProject.name = 'quilt-loom-parent'

include 'quilt-loom'
include 'bootstrap'
project(":bootstrap").projectDir = file("quilt-loom/bootstrap")